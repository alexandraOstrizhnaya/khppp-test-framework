package khppp.application.components;

import khppp.application.sqlScript.Scripts;
import khppp.application.sqlScript.SqlRunner;
import khppp.application.steps.AddUserSteps;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

import java.util.LinkedList;
import java.util.PriorityQueue;
import java.util.Queue;

public class Test implements Scripts {
    public static void main(String[] args) {
        SqlRunner sqlRunner = new SqlRunner();
//        sqlRunner.runSQLScript(initialScriptFilePath);
//        sqlRunner.runSQLScript(dumpDBFilePath);
//        sqlRunner.runSQLScript(createAllUserRoles);

    }

}

